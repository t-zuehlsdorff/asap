<?php

namespace APHPUnit;

# the maximum number of forks executed in parallel
define('MAX_FORKS', 4);

# the identifier of the queue - if other scripts also use queues,
# make sure this identifier is unique!
define('QUEUE_IDENTIFIER', 1);

